# ~/.bashrc.local — loaded LAST by ~/.bashrc generated by dev-bootstrap.
# Good place for personal overrides; never committed by dev-bootstrap.

# Alias examples
# alias gs='git status'
# alias gp='git pull'
# alias dcu='docker compose up -d'

# Per-machine PATH additions
# export PATH="$HOME/work/scripts:$PATH"

# Personal env vars
# export OPENAI_API_KEY=...   # prefer a secret manager in practice
